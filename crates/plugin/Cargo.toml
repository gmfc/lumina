[package]
name = "editor-plugin"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "editor_plugin"

[features]
proptests = []

[dependencies]
editor-core = { path = "../core" }
